# Port used by the Express API server (defaults to 4000 if not set)
API_PORT=4000

# Optional fallback for the API port in some deployment environments
PORT=4000

# Optional path for the SQLite database file
# In production, Easypanel should mount a persistent volume at /data
INFIDASH_DB_PATH=/data/infidash.sqlite

# Optional directory for SQLite backups
INFIDASH_BACKUP_DIR=/data/backups

# Optional frontend/runtime settings
APP_URL=http://127.0.0.1:3000
